<div class="card mb-4">
  <div class="card-header">
    <h5 class="mb-0"><i class="fas fa-unlock-alt"></i> Release of Information</h5>
  </div>
  <div class="card-body">
    <p>
      <strong>Signature:</strong>
      <%= application.release_signature.presence || "Not provided" %>
    </p>
    <p>
      <strong>Date Signed:</strong>
      <%= application.release_date&.strftime("%B %d, %Y") || "Not provided" %>
    </p>
    <p>
  <strong>Consent:</strong>
  <%= application.release_signature.present? ? "Given" : "Not provided" %>
    </p>
  </div>
</div>
