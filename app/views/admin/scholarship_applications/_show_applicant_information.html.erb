<div class="card mb-4">
  <div class="card-header">
    <h5 class="mb-0"><i class="fas fa-user"></i> Applicant Information</h5>
  </div>
  <div class="card-body">
    <div class="row">
      <div class="col-md-6">
        <p><strong>First Name:</strong> <%= application.first_name.presence || "Not provided" %></p>
        <p><strong>Middle Initial:</strong> <%= application.middle_initial.presence || "Not provided" %></p>
        <p><strong>Last Name:</strong> <%= application.last_name.presence || "Not provided" %></p>
        <p><strong>Previous/Maiden Name:</strong> <%= application.previous_maiden_name.presence || "N/A" %></p>
        <p><strong>Date of Birth:</strong> <%= application.date_of_birth&.strftime("%B %d, %Y") || "Not provided" %></p>
        <p><strong>Place of Birth:</strong> <%= application.place_of_birth.presence || "Not provided" %></p>
        <p><strong>Marital Status:</strong> <%= application.marital_status.presence || "Not provided" %></p>
        <p><strong>Number of Dependents:</strong> <%= application.number_of_dependents.presence || "0" %></p>
      </div>
      <div class="col-md-6">
        <p><strong>Email:</strong> <%= application.email_address.presence || "Not provided" %></p>
        <p><strong>Phone (Permanent):</strong> <%= application.permanent_phone.presence || "Not provided" %></p>
        <p><strong>Phone (School):</strong> <%= application.school_phone.presence || "Not provided" %></p>
        <p><strong>Preferred Contact:</strong> <%= application.preferred_contact.presence || "Not specified" %></p>
        <p><strong>Tribe Enrolled:</strong> <%= application.tribe_enrolled.presence || "Not provided" %></p>
        <p><strong>Previous STA Scholarship:</strong> <%= application.previous_scholarship.presence || "Not specified" %></p>
        <p><strong>Year of Previous Scholarship:</strong> <%= application.previous_scholarship_year.presence || "Not provided" %></p>
      </div>
    </div>
  </div>
</div>
